{% block content %}
    <h1>Edit MahasiswaProfile Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_update_mahasiswa_profile', {'id': mahasiswa_profile.id}) }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
